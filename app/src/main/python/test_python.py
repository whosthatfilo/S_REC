
def main(number1,number2):

    num1 = int(number1)         #here we convert number to int for addition
    num2 = int(number2)

    summ = num1 + num2

    return "sum is "+str(summ)

# so our script is ready, this script main funciton will receive number1 and 2 as string
# then we convert it intoint then we add these number and finally return with sum(in string)
    
